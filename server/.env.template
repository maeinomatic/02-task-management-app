### Database connection (Postgres)
# Replace values with your local/dev database credentials
DATABASE_URL=postgresql://devuser:devpass@localhost:5432/taskdb

# When true the server will use the in-memory repositories instead of Prisma/Postgres
USE_IN_MEMORY=false
